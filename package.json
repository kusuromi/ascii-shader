{
  "name": "ascii-shader-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run build:packages && npm run dev -w ascii-shader-demo",
    "build": "npm run build:packages && npm run build -w ascii-shader-demo",
    "build:packages": "npm run build -w @ascii-shader/core && npm run build -w @ascii-shader/renderer && npm run build -w @ascii-shader/react",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "node scripts/clean.mjs"
  },
  "engines": {
    "node": ">=20.19"
  }
}
